On demarre la Phase 2.0 du projet Inaricom en mode autonome full permissions avec Opus 4.7.

============================================================
BOOTSTRAP TOKEN-SAVERS (a faire EN TOUT PREMIER)
============================================================

Installe les plugins token-saving AVANT tout le reste :

1. Plugin token-optimizer (alexgreensh) - deja clone dans ~/.claude/plugins/cache/token-optimizer
   Commande : `/plugin marketplace add alexgreensh/token-optimizer`
   Puis : `/plugin install token-optimizer@alexgreensh-token-optimizer`
   Puis active l'auto-update via /plugin > Marketplaces tab.

2. Active les hooks Token Optimizer en verifiant qu'ils sont bien charges avec `/plugin list`.

3. Verifie que context-mode MCP est bien reconnu :
   - Le serveur MCP `context-mode` est deja dans .mcp.json
   - Doit apparaitre dans la liste MCP au demarrage
   - Test : lister les tools MCP disponibles et confirmer `context-mode` present

4. Verifie RTK :
   - Binaire present : `rtk --version` doit retourner 0.37.2
   - Hook PreToolUse Bash actif via settings.json (ne pas y toucher)

5. Verifie caveman skill : existe dans .claude/skills/caveman/SKILL.md (chargera la verbosite reduite)

Si l'un de ces 4 outils ne marche pas, log le probleme dans `.claude/logs/phase2-0-bootstrap.md` et continue quand meme - pas de blocage sur les optimisations. Mission prioritaire = Phase 2.0, les token-savers sont un bonus.

============================================================
REGLES TOKEN-FRUGAL (applicables tout du long)
============================================================

- Ne lis pas deux fois le meme fichier dans une session (cache prompt Anthropic s'en occupe, et Token Optimizer traque)
- Grep > Read quand tu cherches une ligne precise
- Ne lis JAMAIS en entier les SKILL.md non-actifs (juste le front-matter YAML si besoin)
- Reponds en francais decontracte, pas de preambule type "Je vais maintenant..."
- Pas de `git status` ou `ls` repetitifs : une fois au debut, une fois avant commit
- Batch les operations shell : `cmd1 && cmd2 && cmd3` en 1 tool call
- Quand tache finie, stop. Pas de recapitulatif complaisant.

============================================================
IMPORTANT - ETAT ACTUEL DU WORKSPACE (2026-04-21 22:00)
============================================================

Une session precedente a travaille sur le setup React islands.

[NOTE 21/04/2026] Toute reference a une "Etape 0 cleanup reliquats logo" dans ce prompt est OBSOLETE. La regle logo a ete corrigee : 5 logos thematiques immutables (rouge/or/vert/bleu/neutre) avec swap par [data-theme], voir .claude/rules/logo-immutable.md. Les exemples et etats cibles lies a l'Etape 0 plus bas sont a ignorer.

Premiere action obligatoire APRES bootstrap : faire un `git status` et `git log -5 --oneline` pour cartographier ce qui est deja fait.

ETAT REEL CONNU (DEJA FAIT, NE PAS REFAIRE) :
- [OBSOLETE - NE PAS EXECUTER] Les modifs CSS "Etape 0" : ignorer, la regle logo a change
- `scripts/_fill_env_staging.py` : cree
- pnpm v10.33.0 installe + config securite globale (ignore-scripts, min-release-age 7j, strict-dep-builds)
- `.npmrc` user cree dans `C:\Users\gimu8\.npmrc`
- `.claude/rules/npm-security.md` creee et committee
- `CLAUDE.md` MAJ avec 3 guardrails pnpm
- Brief `docs/BRIEF_CLAUDE_CODE_PHASE_2.0.md` reecrit avec pnpm
- `react-islands/` existe avec scaffold Vite (mais node_modules + package-lock.json SUPPRIMES : il faut refaire un pnpm install propre)
- Opus 4.7 verrouille dans settings.json
- 4 outils token-savers installes (token-optimizer, context-mode, caveman, RTK)

RESTE A FAIRE :
1. [OBSOLETE] Etape 0 cleanup logo - NE PAS FAIRE, regle changee (voir logo-immutable.md)
2. Etape 1-15 setup Vite via pnpm (react-islands/ existe mais il faut pinner versions + pnpm install + audit)

============================================================
REGLE D'IDEMPOTENCE : VERIFIER L'ETAT CIBLE, PAS L'ACTION
============================================================

Pour chaque step : verifie "est-ce que l'ETAT FINAL VOULU est deja atteint" (pas "est-ce que j'ai fait la commande"). Si atteint, skip. Sinon, agis.

Exemples :

[EXEMPLES CLEANUP LOGO RETIRES : obsoletes, ne pas executer - voir note plus haut]

- Projet Vite existe ? `test -d react-islands && test -f react-islands/package.json`
  oui -> verifier contenu, completer ce qui manque (pin versions, pnpm install, audit). non -> `pnpm create vite@latest`.

Applique cette logique A CHAQUE step. Loggue les skips dans `.claude/logs/phase2-0-skipped.md` avec raison.

============================================================
CONTEXTE A CHARGER DANS L'ORDRE
============================================================

1. @CLAUDE.md
2. @.claude/rules/logo-immutable.md
3. @.claude/rules/palette-locked.md
4. @.claude/rules/surgical-fixes.md
5. @.claude/rules/css-custom-properties.md
6. @.claude/rules/security-first.md
7. @.claude/rules/npm-security.md (OBLIGATOIRE - regle supply chain)
8. @docs/phase2-react-islands.md
9. @docs/BRIEF_CLAUDE_CODE_PHASE_2.0.md (MAJ pnpm)
10. @.claude/skills/ACTIVATION.md

SKILLS A ACTIVER :
- react-best-practices (Vercel)
- tailwind-v4-shadcn (secondsky)
- taste-skill (VARIANCE 7, MOTION 6, DENSITY 4)
- caveman (verbosity reduite automatique)

OUTILS :
- /chrome (credentials staging memorises dans Chrome)
- Sous-agent frontend-kadence
- SSH via alias inaricom
- Scripts serveur avec kses_remove_filters
- RTK pour toutes commandes Bash (hook auto)
- context-mode MCP pour sandboxer les outputs lourds
- token-optimizer pour tracking live

MISSION : converger vers l'etat cible final Phase 2.0 en autonomie.

============================================================
ETAT CIBLE FINAL A ATTEINDRE
============================================================

BOOTSTRAP (nouveau) - ETAT CIBLE :
[ ] /plugin list montre token-optimizer actif
[ ] /mcp list montre context-mode operationnel
[ ] rtk --version OK (0.37.2)
[ ] caveman skill detecte

ETAPE 0 (cleanup logo) - OBSOLETE - NE PAS EXECUTER :
[OBSOLETE] Cette section est basee sur une regle fausse (logo unique rouge partout).
          La vraie regle est 5 logos thematiques immutables avec swap par [data-theme].
          Voir .claude/rules/logo-immutable.md. Ne pas executer les items ci-dessous :
          - snippet 63, snippet 347, staging, PNG suppression, commit cleanup = TOUT IGNORER.

ETAPES 1-15 (setup Vite via pnpm) - ETAT CIBLE :
[ ] react-islands/ existe avec template Vite React-TS (OK existe deja)
[ ] react-islands/package.json : versions EXACTES (pas de ^ ni ~)
[ ] react-islands/pnpm-lock.yaml existe et ne contient PAS axios@1.14.1, axios@0.30.4, plain-crypto-js
[ ] react-islands/.npmrc avec ignore-scripts=true + min-release-age=10080 + audit-level=moderate
[ ] pnpm audit --prod --audit-level=moderate = "No known vulnerabilities"
[ ] vite.config.ts configure multi-entry + outDir vers ../plugins/inaricom-core/assets/react
[ ] tsconfig.json avec alias @/ vers ./src
[ ] src/styles/globals.css avec @theme + tokens --inari-*
[ ] shadcn/ui initialise (New York, Neutral, CSS variables) : components.json existe
[ ] Dossiers crees : src/islands, src/components/{ui,hero,cards,layout}, src/hooks, src/lib
[ ] src/islands/homepage.tsx test minimal
[ ] index.html preview avec body.theme-neutre + data-theme=neutre
[ ] pnpm dev fonctionne (localhost:5173, HMR actif)
[ ] pnpm build genere plugins/inaricom-core/assets/react/ avec manifest.json
[ ] .gitignore inclut react-islands/node_modules, dist, .vite (deja fait)
[ ] Git : commit "Phase 2.0: setup Vite + React 19 + Tailwind v4 + shadcn/ui (pnpm-secure)" pushe

============================================================
REGLES ABSOLUES
============================================================

- Logo IMMUTABLE
- Palette 5 themes VERROUILLEE
- Corrections chirurgicales
- SSH via alias inaricom
- Scripts serveur avec kses_remove_filters
- JAMAIS `npm install` -> toujours `pnpm`
- Versions EXACTES (pas de ^ ou ~)
- `pnpm audit --prod` OBLIGATOIRE apres chaque `pnpm add`
- Verifier absence plain-crypto-js / axios@1.14.1 / axios@0.30.4 dans pnpm-lock.yaml

============================================================
GESTION D'INCIDENTS
============================================================

pnpm audit CRITICAL ou HIGH -> STOP immediat, rollback, rapport dans .claude/logs/phase2-0-security-alert.md
Package bloque par min-release-age -> version N-1 stable, log dans .claude/logs/phase2-0-issues.md
Autre probleme non-securite -> log et continuer en mode degrade si possible

============================================================
LIVRABLE FINAL
============================================================

A la fin, ecrire `.claude/logs/phase2-0-completion-report.md` :
- Tous les items [x] de l'etat cible (coches/non avec raison)
- Liste commits faits (hashes + messages)
- Liste exhaustive deps installees (package + version exacte)
- Resultat pnpm audit --prod
- Resultat grep plain-crypto-js + axios compromis sur pnpm-lock.yaml
- URL preview (localhost:5173) + test accessibilite
- Build output path verifie
- Items skip dans .claude/logs/phase2-0-skipped.md avec raisons
- Rapport token savings (via /cost ou token-optimizer dashboard)
- Prochaines etapes Phase 2.1

Go.
